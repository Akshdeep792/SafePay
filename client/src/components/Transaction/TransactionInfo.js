import Wrapper from '../../assets/wrappers/TransactionInfo'

const TransactionInfo = ({ icon, text }) => {
  return (
    <Wrapper>
      <span className='icon'>{icon}</span>
      <span className='text'>{text}</span>
    </Wrapper>
  )
}

export default TransactionInfo